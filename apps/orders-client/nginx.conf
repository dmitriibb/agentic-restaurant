server {
    listen 80;
    server_name _;

    # Serve the React SPA
    root /usr/share/nginx/html;
    index index.html;

    # ---------- Reverse-proxy rules for backend APIs ----------
    # Route by the second path segment to the correct service.

    location /api/v1/auth/ {
        proxy_pass         http://users-service:8081;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location /api/v1/menu-items {
        proxy_pass         http://menu-service:8082;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    location /api/v1/orders {
        proxy_pass         http://orders-service:8083;
        proxy_set_header   Host              $host;
        proxy_set_header   X-Real-IP         $remote_addr;
        proxy_set_header   X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header   X-Forwarded-Proto $scheme;
    }

    # ---------- SPA fallback ----------
    # Any route not matching /api/* falls back to index.html
    # so that React Router handles client-side routing.
    location / {
        try_files $uri $uri/ /index.html;
    }
}
