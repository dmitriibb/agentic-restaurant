spring:
  datasource:
    url: jdbc:mysql://localhost:3307/orders_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
    username: orders
    password: orders

management:
  endpoint:
    health:
      show-details: always
